import 'package:flutter/material.dart';

// ? new approch in kareforce to use colors so examine this
class AppColors {

  AppColors._();

  // Splash Screen
  static const Color primary = Color.fromRGBO(0, 91, 191, 1);
  static const Color blue = Colors.blue;
  static const Color white = Colors.white;
  static const Color black = Colors.black87;

  //? App Primary Colors.
  static const Color orangePrimary = Color(0xFFFF7A33);
  static const Color orangeDark = Color(0xFFE45D1A);
  static const Color orangeLight = Color(0xFFFFB38A);
  static const Color backGround = Color(0xFFFFA092);


}